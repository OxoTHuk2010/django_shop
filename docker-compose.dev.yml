services:
  db:
    ports:
      - "5432:5432"

  web:
    command: python manage.py runserver 0.0.0.0:8000 --noreload
    ports:
      - "8000:8000"
    environment:
      DEBUG: "1"

  nginx:
    profiles:
      - never
